language: python
cache:
  pip: true
  directories:
    - $HOME/.cache/pre-commit
python:
  - "3.7.4"
install:
  - pip install -r requirements.txt
before_script:
  - pre-commit run --all-files
script:
  - pytest
